package com.obd.insight.domain.model

enum class BluetoothError {
    DEVICE_NOT_FOUND,
    PERMISSION_DENIED,
    BLUETOOTH_OFF,
    DISCOVERY_FAILED,
    CONNECTION_TIMEOUT,
    SOCKET_ERROR,
    IO_ERROR,
    PROTOCOL_ERROR,
    UNKNOWN
}
